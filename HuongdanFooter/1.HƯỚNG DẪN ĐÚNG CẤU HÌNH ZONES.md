# HƯỚNG DẪN ĐÚNG CẤU HÌNH ZONES TRONG ORCHARDCORE

## 🎯 **MỤC TIÊU**
Hướng dẫn chi tiết cách cấu hình zones (vùng) trong OrchardCore để tạo ra các khu vực footer riêng biệt.

## 📍 **BƯỚC 1: TRUY CẬP ADMIN PANEL**
1. Mở trình duyệt web
2. Truy cập địa chỉ: `http://localhost:41481/Admin`
3. Đăng nhập với tài khoản admin đã tạo

## 📍 **BƯỚC 2: VÀO MỤC ZONES**
1. Trong Admin Panel, tìm menu bên trái
2. Click vào **"Design"** để mở rộng menu
3. Click vào **"Settings"** 
4. Click vào **"Zones"**

**Đường dẫn trực tiếp:** `http://localhost:41481/Admin/Settings/Zones`

## 📍 **BƯỚC 3: CẤU HÌNH ZONES**
1. Trong trang Settings/Zones, tìm phần **"Available zones for Layers"**
2. Sẽ thấy ô text input có sẵn nội dung: `Content`
3. **Thêm zones mới** bằng cách:
   - Đặt con trở chuột vào cuối từ `Content`
   - Thêm dấu phẩy và khoảng trắng: `, `
   - Gõ thêm: `FooterLeft, FooterCenter, FooterRight`
   - **Kết quả cuối cùng:** `Content, FooterLeft, FooterCenter, FooterRight`

## 📍 **BƯỚC 4: LƯU CẤU HÌNH**
1. Click nút **"Save"** màu xanh ở cuối trang
2. Hệ thống sẽ hiển thị thông báo thành công
3. Trang sẽ reload và hiển thị zones đã được cập nhật

## ✅ **KẾT QUẢ SAU KHI CẤU HÌNH**
- Có tổng cộng **4 zones** có thể sử dụng:
  - `Content` - Zone chính cho nội dung
  - `FooterLeft` - Zone footer bên trái
  - `FooterCenter` - Zone footer ở giữa  
  - `FooterRight` - Zone footer bên phải

## 🔍 **KIỂM TRA ZONES ĐÃ TẠO**
1. Vào **Design** → **Widgets**
2. Khi tạo widget mới, sẽ thấy dropdown **"Zone"** có 4 lựa chọn:
   - Content
   - FooterLeft
   - FooterCenter
   - FooterRight

## ⚠️ **LƯU Ý QUAN TRỌNG**
- Zones chỉ là **định nghĩa tên**, chưa hiển thị trên website
- Để hiển thị zones, cần **render trong Layout.liquid**
- Cú pháp render: `{% render_zone "TenZone" %}`
- Ví dụ: `{% render_zone "FooterLeft" %}`

## 🎯 **BƯỚC TIẾP THEO**
Sau khi cấu hình zones, cần:
1. Cập nhật file **Layout.liquid** để render các zones
2. Tạo **widgets** và gán vào từng zone
3. Tạo **CSS** để styling cho footer

---
**Ghi chú:** Hướng dẫn này được tạo dựa trên OrchardCore và đã được kiểm tra thực tế.